export class Diagnostico {
    id!: number;     // <-- Adiciona esta linha
    nome!: string;   // <-- Adiciona esta linha
    cid?: string;    // <-- Adiciona esta linha (opcional)
    sigla?: string;  // <-- Adiciona esta linha (opcional)
}
